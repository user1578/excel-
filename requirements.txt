# Windows 本地桌面工具运行依赖。
PySide6>=6.7,<7.0
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
# 模板生成与填充时校验、等比例限制 PNG/JPG 照片。
Pillow>=10.0,<13.0
# Windows 下的 Excel COM 高保真模板填写；其它平台启动不依赖它。
pywin32>=306; sys_platform == "win32"

# 自动化测试依赖。
pytest>=8.0,<9.0
